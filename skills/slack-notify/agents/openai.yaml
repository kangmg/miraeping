interface:
  display_name: "Slack Notify"
  short_description: "Minimal Slack alerts for scheduler, Python, and GPU jobs"
  default_prompt: "Use $slack-notify to check my setup and add a concise completion or failure alert with the calculation path."
